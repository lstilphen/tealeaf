<h1>Welcome to Blackjack!</h1>

<div class="well" id = "dealer">
  <h4>Dealer's Cards</h4>
  <ul>
    <% session[:dealer_cards].each do |card| %>
      <li><%= card.to_s %></li>
    <% end %>
  </ul>
</div>

<div class="well" id = "player">
  <h4> <%= session[:player] %>'s cards</h4>
  <ul>
    <% session[:player_cards].each do |card| %>
      <li><%= card.to_s %></li>
    <% end %>
  </ul>
</div>

<h4> <%= session[:player] %> has totalcards. What would you like to do?</h4>
<p class = "muted"><%= session[:player] %> has total$. Bet for this round is bet$.</hp>


<form action = '/hit' method = "post">
<input type = "submit" value = "HIT" class="btn btn-success btn-large">
</form>
<form action = '/stay' method = "post">
<input type = "submit" value = "STAY" class="btn btn-warning btn-large">
</form>
